<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.gds.contract.dao.HotelContractDao">
    
	<sql id="hotelContractColumns">
		a.id AS "id",
		a.hotelid AS "hotelid",
		a.hotelpms AS "hotelpms",
		a.author AS "author",
		a.status AS "status",
		a.type AS "type",
		a.content AS "content",
		a.createtime AS "createtime",
		a.version AS "version"
	</sql>
	
	<sql id="hotelContractJoins">
	</sql>
    
	<select id="get" resultType="HotelContract">
		SELECT 
			<include refid="hotelContractColumns"/>
		FROM hotel_contract a
		<include refid="hotelContractJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="HotelContract">
		SELECT 
			<include refid="hotelContractColumns"/>
		FROM hotel_contract a
		<include refid="hotelContractJoins"/>
		<where>
			
			<if test="hotelid != null and hotelid != ''">
				AND a.hotelid = #{hotelid}
			</if>
			<if test="hotelpms != null and hotelpms != ''">
				AND a.hotelpms = #{hotelpms}
			</if>
			<if test="author != null and author != ''">
				AND a.author = #{author}
			</if>
			<if test="status != null and status != ''">
				AND a.status = #{status}
			</if>
			<if test="type != null and type != ''">
				AND a.type = #{type}
			</if>
			<if test="createtime != null and createtime != ''">
				 AND date_format(a.createtime,'%Y-%m-%d') = date_format(#{createtime},'%Y-%m-%d')
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			 ORDER BY a.version DESC
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="HotelContract">
		SELECT 
			<include refid="hotelContractColumns"/>
		FROM hotel_contract a
		<include refid="hotelContractJoins"/>
		<where>
			
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			 ORDER BY a.version DESC
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO hotel_contract(
			hotelid,
			hotelpms,
			author,
			status,
			type,
			content,
			createtime,
			version
		) VALUES (
			#{hotelid},
			#{hotelpms},
			#{author},
			#{status},
			#{type},
			#{content},
			#{createtime},
			#{version}
		)
	</insert>
	
	<update id="update">
		UPDATE hotel_contract SET 	
			hotelid = #{hotelid},
			hotelpms = #{hotelpms},
			author = #{author},
			status = #{status},
			type = #{type},
			content = #{content},
			createtime = #{createtime},
			version = #{version}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		DELETE FROM hotel_contract
		WHERE id = #{id}
	</update>
	
</mapper>