<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.gds.datacenter.dao.OtaOrderdetailDao">
    
	<sql id="otaOrderdetailColumns">
		a.id AS "id",
		a.orderid AS "orderid",
		a.roomtypeid AS "roomtypeid",
		a.booknum AS "booknum",
		a.createtime AS "createtime",
		a.updatetime AS "updatetime"
	</sql>
	
	<sql id="otaOrderdetailJoins">
	</sql>
    
	<select id="get" resultType="OtaOrderdetail">
		SELECT 
			<include refid="otaOrderdetailColumns"/>
		FROM ota_orderdetail a
		<include refid="otaOrderdetailJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="com.thinkgem.gds.datacenter.entity.OtaOrderdetail">
		SELECT 
			<include refid="otaOrderdetailColumns"/>
		FROM ota_orderdetail a
		<include refid="otaOrderdetailJoins"/>
		<where>						
			<if test="orderid != null and orderid != ''">
				AND a.orderid = #{orderid}
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="com.thinkgem.gds.datacenter.entity.OtaOrderdetail">
		SELECT 
			<include refid="otaOrderdetailColumns"/>
		FROM ota_orderdetail a
		<include refid="otaOrderdetailJoins"/>
		<where>
			
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO ota_orderdetail(
			id,
			orderid,
			roomtypeid,
			booknum,
			createtime,
			updatetime
		) VALUES (
			#{id},
			#{orderid},
			#{roomtypeid},
			#{booknum},
			#{createtime},
			#{updatetime}
		)
	</insert>
	
	<update id="update">
		UPDATE ota_orderdetail SET 	
			orderid = #{orderid},
			roomtypeid = #{roomtypeid},
			booknum = #{booknum},
			createtime = #{createtime},
			updatetime = #{updatetime}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		DELETE FROM ota_orderdetail
		WHERE id = #{id}
	</update>
	
</mapper>