<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.sc.base.dao.SettlementBaseGoodsrateDao">
    
	<sql id="settlementBaseGoodsrateColumns">
		a.id AS "id",
		a.classname AS "classname",
		a.gcode AS "gcode",
		a.gname AS "gname",
		a.ratepercent AS "ratepercent"
	</sql>
	
	<sql id="settlementBaseGoodsrateJoins">
	</sql>
    
	<select id="get" resultType="SettlementBaseGoodsrate">
		SELECT 
			<include refid="settlementBaseGoodsrateColumns"/>
		FROM settlement_base_goodsrate a
		<include refid="settlementBaseGoodsrateJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="SettlementBaseGoodsrate">
		SELECT 
			<include refid="settlementBaseGoodsrateColumns"/>
		FROM settlement_base_goodsrate a
		<include refid="settlementBaseGoodsrateJoins"/>
		<where>
			
			<if test="classname != null and classname != ''">
				AND a.classname = #{classname}
			</if>
			<if test="gcode != null and gcode != ''">
				AND a.gcode = #{gcode}
			</if>
			<if test="gname != null and gname != ''">
				AND a.gname LIKE 
					<if test="dbName == 'oracle'">'%'||#{gname}||'%'</if>
					<if test="dbName == 'mssql'">'%'+#{gname}+'%'</if>
					<if test="dbName == 'mysql'">concat('%',#{gname},'%')</if>
			</if>
			<if test="beginRatepercent != null and endRatepercent != null and beginRatepercent != '' and endRatepercent != ''">
				AND a.ratepercent BETWEEN #{beginRatepercent} AND #{endRatepercent}
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="SettlementBaseGoodsrate">
		SELECT 
			<include refid="settlementBaseGoodsrateColumns"/>
		FROM settlement_base_goodsrate a
		<include refid="settlementBaseGoodsrateJoins"/>
		<where>
			
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO settlement_base_goodsrate(
			id,
			classname,
			gcode,
			gname,
			ratepercent
		) VALUES (
			#{id},
			#{classname},
			#{gcode},
			#{gname},
			#{ratepercent}
		)
	</insert>
	
	<update id="update">
		UPDATE settlement_base_goodsrate SET 	
			classname = #{classname},
			gcode = #{gcode},
			gname = #{gname},
			ratepercent = #{ratepercent}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		DELETE FROM settlement_base_goodsrate
		WHERE id = #{id}
	</update>
	
</mapper>