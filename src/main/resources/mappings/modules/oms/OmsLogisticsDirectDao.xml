<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.jeesite.modules.oms.dao.OmsLogisticsDirectDao">
    
	<sql id="omsLogisticsDirectColumns">
		a.id AS "id",
		a.vendor_code AS "vendorCode",
		a.vendor_name AS "vendorName",
		a.commodity_code AS "commodityCode",
		a.commodity_name AS "commodityName",
		a.distinct_code AS "distinctCode",
		a.distinct_name AS "distinctName",
		a.s_flag AS "sFlag"
	</sql>
	
	<sql id="omsLogisticsDirectJoins">
	</sql>
    
	<select id="get" resultType="OmsLogisticsDirect">
		SELECT 
			<include refid="omsLogisticsDirectColumns"/>
		FROM oms_logistics_direct a
		<include refid="omsLogisticsDirectJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="OmsLogisticsDirect">
		SELECT 
			<include refid="omsLogisticsDirectColumns"/>
		FROM oms_logistics_direct a
		<include refid="omsLogisticsDirectJoins"/>
		<where>
			<if test="commodityCode != null and commodityCode != '' ">
				AND a.COMMODITY_CODE = #{commodityCode}
			</if>

			<if test="commodityName != null and commodityName != '' ">
				AND a.COMMODITY_NAME = #{commodityName}
			</if>
			
			<if test="distinctCode != null and distinctCode != '' ">
				AND a.DISTINCT_CODE = #{distinctCode}
			</if>
			
			<if test="distinctName != null and distinctName != '' ">
				AND a.DISTINCT_NAME = #{distinctName}
			</if>									
			
			
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="OmsLogisticsDirect">
		SELECT 
			<include refid="omsLogisticsDirectColumns"/>
		FROM oms_logistics_direct a
		<include refid="omsLogisticsDirectJoins"/>
		<where>
			<if test="commodityCode != null and commodityCode != '' ">
				AND a.COMMODITY_CODE = #{commodityCode}
			</if>

			<if test="commodityName != null and commodityName != '' ">
				AND a.COMMODITY_NAME = #{commodityName}
			</if>
			
			<if test="distinctCode != null and distinctCode != '' ">
				AND a.DISTINCT_CODE = #{distinctCode}
			</if>
			
			<if test="distinctName != null and distinctName != '' ">
				AND a.DISTINCT_NAME = #{distinctName}
			</if>					
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO oms_logistics_direct(
			id,
			vendor_code,
			vendor_name,
			commodity_code,
			commodity_name,
			distinct_code,
			distinct_name,
			s_flag
		) VALUES (
			#{id},
			#{vendorCode},
			#{vendorName},
			#{commodityCode},
			#{commodityName},
			#{distinctCode},
			#{distinctName},
			#{sFlag}
		)
	</insert>
	
	<update id="update">
		UPDATE oms_logistics_direct SET 	
			vendor_code = #{vendorCode},
			vendor_name = #{vendorName},
			commodity_code = #{commodityCode},
			commodity_name = #{commodityName},
			distinct_code = #{distinctCode},
			distinct_name = #{distinctName},
			s_flag = #{sFlag}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		DELETE FROM oms_logistics_direct
		WHERE id = #{id}
	</update>
	
</mapper>