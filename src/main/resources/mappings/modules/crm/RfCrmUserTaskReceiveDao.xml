<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.crm.task.dao.sub.RfCrmUserTaskReceiveDao">
    
	<sql id="rfCrmUserTaskReceiveColumns">
		a.id AS "id",
		a.receive_user_id AS "receiveUserId",
		a.task_id AS "taskId",
		a.is_complete AS "isComplete",
		a.remark AS "remark",
		a.create_time AS "createTime",
		a.update_time AS "updateTime"
	</sql>
	
	<sql id="rfCrmUserTaskReceiveJoins">
	</sql>
    
	<select id="get" resultType="RfCrmUserTaskReceive">
		SELECT 
			<include refid="rfCrmUserTaskReceiveColumns"/>
		FROM rf_crm_user_task_receive a
		<include refid="rfCrmUserTaskReceiveJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="RfCrmUserTaskReceive">
		SELECT 
			<include refid="rfCrmUserTaskReceiveColumns"/>
		FROM rf_crm_user_task_receive a
		<include refid="rfCrmUserTaskReceiveJoins"/>
		<where>
			
			<if test="id != null and id != ''">
				AND a.id = #{id}
			</if>
			<if test="receiveUserId != null and receiveUserId != ''">
				AND a.receive_user_id = #{receiveUserId}
			</if>
			<if test="taskId != null and taskId != ''">
				AND a.task_id = #{taskId}
			</if>
			<if test="isComplete != null and isComplete != ''">
				AND a.is_complete = #{isComplete}
			</if>
			<if test="remark != null and remark != ''">
				AND a.remark = #{remark}
			</if>
			<if test="createTime != null and createTime != ''">
				AND a.create_time = #{createTime}
			</if>
			<if test="updateTime != null and updateTime != ''">
				AND a.update_time = #{updateTime}
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="RfCrmUserTaskReceive">
		SELECT 
			<include refid="rfCrmUserTaskReceiveColumns"/>
		FROM rf_crm_user_task_receive a
		<include refid="rfCrmUserTaskReceiveJoins"/>
		<where>
			
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO rf_crm_user_task_receive(
			receive_user_id,
			task_id,
			is_complete,
			remark,
			create_time,
			update_time
		) VALUES (
			#{receiveUserId},
			#{taskId},
			#{isComplete},
			#{remark},
			#{createTime},
			#{updateTime}
		)
	</insert>
	
	<update id="update">
		UPDATE rf_crm_user_task_receive SET 	
			receive_user_id = #{receiveUserId},
			task_id = #{taskId},
			is_complete = #{isComplete},
			remark = #{remark},
			create_time = #{createTime},
			update_time = #{updateTime}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		DELETE FROM rf_crm_user_task_receive
		WHERE id = #{id}
	</update>
	
</mapper>